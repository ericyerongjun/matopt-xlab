/**
 * DocumentPage — upload & view parsed documents.
 *
 * TODO: Add your document viewer UI here.
 */

import React from "react";

export default function DocumentPage() {
    return (
        <div className="document-page">
            <h2>Document Parser</h2>
            <p>Upload a PDF or image to extract Markdown + LaTeX.</p>
            {/* TODO: integrate FileUploader + parsed document display */}
        </div>
    );
}
